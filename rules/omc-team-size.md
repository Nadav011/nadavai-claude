# OMC agent count

- `/team` without an explicit N: 3 teammates. An explicit N always wins.
- autopilot, ralph, execute: up to 3 parallel agents, only when the work splits into independent files or modules. Small or sequential tasks get one.
